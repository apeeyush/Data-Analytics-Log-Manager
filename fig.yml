db:
  image: "postgres:9.3"
  expose:
    - 5432
web:
  build: .
  command: bundle exec puma -C config/puma.rb
  volumes:
    - .:/usr/src/app
  ports:
    - "3000:3000"
  links:
    - db
  environment:
    DOCKER_FIG: true
